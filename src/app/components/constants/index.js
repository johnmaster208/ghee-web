import keyMirror from 'keymirror'

export const ACTION = keyMirror({
    APP_TOGGLE_SIDEBAR: null,
    UNKNOWN: null
})

export const STATUS = keyMirror({
    PENDING: null,
    SUCCESS: null,
    FAILURE: null,
    ERROR: null,
    ABORT: null,
    UNKNOWN: null
})

export const ALERT = keyMirror({
    UNKNOWN: null
})

export const MODAL = keyMirror({
})

